const fs = require('fs');

async function check() {
  const fetch = (await import('node-fetch')).default;
  console.log("Just running a test to see what data looks like, or I can just print the value in the backend");
}
